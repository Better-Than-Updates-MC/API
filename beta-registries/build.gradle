archivesBaseName = 'beta-registries'
version = getSubprojectVersion(project, '1.0.0')

moduleDependencies(project, 'beta-api-base', 'beta-networking')

dependencies {
	testImplementation project(path: ':beta-recipes')
	testImplementation project(path: ':beta-translations')
}
