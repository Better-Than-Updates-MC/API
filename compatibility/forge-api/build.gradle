archivesBaseName = 'forge-mod-loader'
version = getSubprojectVersion(project, '8.0.0')

apply plugin: 'java-library'

moduleDependencies(project, 'beta-api-base', 'forge-mod-loader', 'beta-events')

dependencies {
}