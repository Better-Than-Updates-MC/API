archivesBaseName = 'forge-api'
version = getSubprojectVersion(project, '1.0.4')

apply plugin: 'java-library'

moduleDependencies(project, 'core', 'events')

dependencies {
	include modImplementation("io.github.minecraft-cursed-legacy:legacy-api-base:${rootProject.cursed_api_version}-1.0.0")
}
