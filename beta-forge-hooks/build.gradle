archivesBaseName = 'legacy-forge-hooks'
version = getSubprojectVersion(project, '1.0.4-0a874de')

apply plugin: 'java-library'

moduleDependencies(project,
		'beta-accessibility', 'beta-api-base', 'beta-events', 'beta-content', 'beta-registries', 'beta-forge-patches')

dependencies {
}