archivesBaseName = 'beta-attached-data'
version = getSubprojectVersion(project, '1.0.0')

apply plugin: 'java-library'

moduleDependencies(project, 'beta-api-base')

dependencies {
	testImplementation project(path: ':beta-events')
	testImplementation project(path: ':beta-registries')
}
