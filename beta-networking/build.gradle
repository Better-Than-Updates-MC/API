archivesBaseName = 'beta-networking'
version = getSubprojectVersion(project, '1.0.0')

moduleDependencies(project, 'beta-api-base')
dependencies {
	testImplementation project(path: ':beta-interaction-events')
}
