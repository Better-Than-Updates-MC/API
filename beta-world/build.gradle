archivesBaseName = 'beta-world'
version = getSubprojectVersion(project, '1.0.0')

apply plugin: 'java-library'

moduleDependencies(project, 'beta-api-base', 'beta-attached-data', 'beta-accessibility')

dependencies {
}
