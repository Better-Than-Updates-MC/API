archivesBaseName = 'legacy-networking-v0'
version = getSubprojectVersion(project, '2.0.0')

moduleDependencies(project, 'legacy-api-base')
dependencies {
	testImplementation project(path: ':legacy-interaction-events-v0')
}
